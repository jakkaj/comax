# Fix FX001: Detection & restore correctness pass

**Created**: 2026-05-23
**Status**: Proposed
**Plan**: Standalone (comax has no plan repo; single-file CLI)
**Source**: Validation session 2026-05-22/23 against live tmux state — 12 panes failed to restore correctly post-reboot; subsequent `discover_all` cross-check surfaced an additional false-positive
**Domain(s)**: `discovery`, `restore`, `infra` (functional sub-areas of `src/comax/cli.py` — comax is not modelled as a multi-domain repo)

---

## Problem

Comax saved 45 agents correctly, but `--restore` left **12 panes broken** after reboot. Root causes are three independent bugs that compounded into a silent-failure pattern: (1) a substring-match in copilot detection mistakes any process whose argv merely *mentions* "copilot" for a real copilot, (2) `tmux send-keys -t <session>:<window-name-with-dots>` is misparsed by tmux as a window-pane index expression and the resume command never lands, (3) `run()` discards `stderr` and `returncode` so the restore table prints `OK` even when send-keys failed entirely. On top of those, stale session UUIDs and a shell-init race produce extra silent failures.

## Proposed Fix

Replace the substring detector with an argv[0]/argv[1] binary-name check (covers the `node /path/to/copilot` wrapper case). Route every restore `send-keys` through PID-based pane resolution so window names containing dots no longer break. Make `run()` return `(stdout, stderr, returncode)` and wire the restore table to report `OK`/`WARN`/`FAIL` truthfully. Pre-validate each saved session UUID against the agent's on-disk store before emitting a resume, so stale UUIDs become `WARN`s rather than half-broken panes. Wait for shell readiness (and refresh the pane PID) before sending the resume command, eliminating the new-window race.

## Domain Impact

| Domain     | Relationship | What Changes                                                                                                                        |
|------------|--------------|-------------------------------------------------------------------------------------------------------------------------------------|
| discovery  | Modified     | `find_copilot_in_pane` and `pane_has_agent("copilot", …)` switch from substring to argv-binary identification; new `_is_copilot_process` helper. |
| restore    | Modified     | All `send-keys` paths route through a single `_send_keys_to_pane_by_pid` helper; new pre-resume `is_session_alive(agent_type, uuid)` gate. |
| infra      | Modified     | `run()` now returns a structured result; new `wait_for_shell_ready(pane_id, timeout)` helper; callers updated for new signature.    |

**Risk**: Low–Medium. No external contracts; all changes contained to `src/comax/cli.py`. The `run()` signature change touches every callsite within the file — mechanical but wide. State file (`~/.config/comax/state.json`) schema is unchanged, so existing saves remain restorable post-fix.

## Tasks

| Status | ID       | Task                                                                                                                                                                                                                                                                                                                                                                                            | Domain    | Path(s)                                            | Done When                                                                                                                                                                                                                                                                              | Notes                                                                                                                                                                                                                                                                                  |
|--------|----------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------|----------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| [ ]    | FX001-1  | Make `run()` return `(stdout, stderr, returncode)` (or add `run_checked()` returning a small dataclass). Update every callsite — `get_tmux_panes`, `get_child_pids`, `get_process_command`, `get_process_cwd`, `get_open_file_paths`, `cmd_save`, `cmd_restore`, `_send_keys_to_pane`, etc. Plumb non-zero returncode into the restore table so rows report `OK` / `WARN` / `FAIL` truthfully.   | infra     | `/Users/jordanknight/github/comax/src/comax/cli.py` | A deliberately invalid `tmux send-keys -t nonexistent:0 …` produces a `FAIL` row in the restore output (currently prints `OK`). All existing callers compile and behave unchanged for the happy path.                                                                                | Bug #3 from validation. Do this FIRST — the other tasks need truthful feedback. Result type suggestion: `@dataclass class RunResult: stdout: str; stderr: str; returncode: int`. Add a `.ok` property for ergonomic checks. Keep `run()` returning just `stdout` if the diff churn is too large; introduce `run_full()` instead. |
| [x]    | FX001-2  | Replace copilot substring matching with binary-aware identification. Add `_is_copilot_process(cmd) -> bool` that returns True iff `argv[0]` basename == `copilot`, OR `argv[0]` basename == `node` AND `argv[1]` basename == `copilot`. Use it in `find_copilot_in_pane` (replace the list comprehension on line ~196) and in `pane_has_agent("copilot", …)` (line ~159).                       | discovery | `/Users/jordanknight/github/comax/src/comax/cli.py` | Running `comax` in a pi pane whose child process has the word "copilot" in its argv (e.g. a `lean-ctx -c "…build_copilot_lock_index…"` subshell) no longer mis-classifies the pane as copilot. The legitimate `node /path/to/copilot --yolo --resume <uuid>` case still detects.       | Bugs #1 and #4. Validation reproducer: when this fix dossier was authored, `discover_all()` reported `comax:node [copilot] uuid=None cwd=None` because my own diagnostic shell command contained `build_copilot_lock_index` as a token. After the fix, that pane resolves to pi only. |
| [ ]    | FX001-3  | Consolidate all restore-time `tmux send-keys` calls onto a single `_send_keys_to_pane_by_pid(pane_pid, keys)` helper that resolves the pane PID → `<session>:<window_idx>.<pane_idx>` target via `tmux list-panes -a` and verifies the resolved target survives `tmux send-keys` (returncode check from FX001-1). Remove the name-based `tmux send-keys -t <session>:<window-name>` branches in both the new-session-first-window path and the new-window-fallback path. | restore   | `/Users/jordanknight/github/comax/src/comax/cli.py` | Restoring a fresh tmux session whose first saved window name contains dots (e.g. `2.1.126`, `2.1.138`, `2.1.90`) delivers the resume command. Manually verified: `tmux send-keys -t osk-data:2.0` works, `tmux send-keys -t osk-data:2.1.126` fails with `can't find pane: 1.126`.    | Bug #2 from validation. Affects `012-foc:2.1.126`, `articles:2.1.138`, `chainglass:2.1.145`, `higgs-jordo:2.1.126`, `home-improvement:2.1.90` on every reboot. Also remove the `tmux list-panes -t <session> -a` quirk (the `-a` flag silently ignores `-t`) — use plain `list-panes -a`. |
| [ ]    | FX001-4  | Add `is_session_alive(agent_type, uuid) -> bool` that checks the agent's on-disk store: copilot → `~/.copilot/session-state/<uuid>/` exists, claude → any file under `~/.claude/projects/*/<uuid>.jsonl`, pi → any file matching `~/.pi/agent/sessions/*/*<uuid>*.jsonl`. Gate every resume in `cmd_restore` behind it; on miss, emit a `WARN: uuid stale` row, optionally `cd <cwd>` in the pane, do not send the resume.   | restore   | `/Users/jordanknight/github/comax/src/comax/cli.py` | A saved state pointing to a now-deleted UUID produces `WARN: uuid stale` in the table and leaves a usable shell in the correct cwd — not a pane displaying `Error: No session matched …`.                                                                                            | Bug #5. Validation confirmed claude UUIDs `67975e08` (012-foc) and `c9587512` (osk-data) have zero matching files on disk yet were sent as resume targets anyway, producing the broken panes the user noticed.                                                                       |
| [ ]    | FX001-5  | After `tmux new-session` / `new-window`, poll `tmux display-message -t <pane> -p '#{pane_current_command}'` until it equals the user shell (`zsh`/`bash`) — bounded by a 2 s wall-clock timeout. Refresh `pane_pid` from `tmux list-panes` immediately before sending the resume command. Then route through `_send_keys_to_pane_by_pid` from FX001-3.                                          | restore   | `/Users/jordanknight/github/comax/src/comax/cli.py` | On a machine with a deliberately slow `.zshrc` (e.g. `sleep 0.5` inserted), restore still reliably delivers the resume command — no lost first character, no `cd: command not found` from typing into a half-initialised prompt.                                                       | Bugs #9 (PID staleness after `new-window`) and #10 (send-keys race with shell init). Both are theoretically present today; rarely observed in practice but cheap to close once FX001-3 has consolidated the send-keys path.                                                          |

## Workshops Consumed

None — comax does not maintain a workshops/ directory.

## Acceptance

- [ ] **Truthful restore**: invoking `comax --restore` with a fake state.json entry pointing at a nonexistent pane produces a `FAIL` row in the table (currently prints `OK`). _(FX001-1 acceptance)_
- [x] **No copilot false-positives**: running `comax --save` while a pi pane has a child whose argv contains the word "copilot" (verifiable with `bash -c 'echo copilot; sleep 30' &` inside a pi pane) saves that pane as `pi`, not `copilot`. _(FX001-2 acceptance — validated 2026-05-23 via reproducer + 11-case unit table + 35-agent regression sweep)_
- [ ] **Dotted-name restore works**: a freshly saved state containing a window named `2.1.126` restores into a tmux session of the same name and runs its claude/pi/copilot resume command in that window. _(FX001-3 acceptance)_
- [ ] **Stale-UUID handling**: state.json with a claude UUID whose `~/.claude/projects/*/<uuid>.jsonl` is absent produces a `WARN: uuid stale` row and leaves a clean shell in the saved cwd. _(FX001-4 acceptance)_
- [ ] **Race-free restore**: with a 500 ms-delayed `.zshrc`, restore still delivers an intact `cd '<cwd>' && <agent> --resume <uuid>` command. _(FX001-5 acceptance)_
- [ ] **Regression budget**: the 33 windows that restored correctly under today's binary continue to restore correctly under the fixed binary; no new `FAIL` or `WARN` rows attributable to the fix.
- [ ] **Manual end-to-end smoke**: after `uv tool install . --force`, the operator runs `comax`, then closes a representative window (e.g. `articles:2.1.138`), then runs `comax --restore`, and observes the window reappears with the agent live (or `WARN: uuid stale` if the UUID has rotated since save).

## Out of Scope (deferred to follow-up fixes)

- **FX002 (proposed)**: Save tmux window topology even when no agent is live (bug #6 — a tmux window the user had quit before save is currently lost). Schema change to state.json.
- **FX003 (proposed)**: Cache `ps -eo pid=,ppid=` once per `discover_all` call (bug #7 — `walk_process_tree` re-runs the full process listing per node). Performance fix.
- **FX004 (proposed)**: Watch/daemon mode that periodically `--save`s without operator action (bug #11). Feature, not a fix.
- **Documented limitation**: pi rewrites argv[0] to `"pi"`, so launch flags (model, thinking, extensions) cannot be recovered on restore (bug #8). README already notes this; no comax-side fix is possible.

## Discoveries & Learnings

_Populated during implementation by `plan-6-v2-implement-phase`._

| Date | Task | Type | Discovery | Resolution |
|------|------|------|-----------|------------|
| 2026-05-23 | FX001-2 | decision | Two substring-match callsites needed updating, not just `find_copilot_in_pane`: also `pane_has_agent("copilot", …)` at line 159, used by `cmd_restore` to skip already-running agents. Without fixing both, restore could SKIP resuming a real copilot pane on the strength of a stray child argv containing "copilot". | Added a single `_is_copilot_process(cmd)` helper and used it in both callsites. |
| 2026-05-23 | FX001-2 | insight | The `node /path/to/copilot` wrapper pattern means we must look at argv[0]+argv[1], not argv[0] alone. Helper accounts for that explicitly. | Closed-loop validation includes a `node /Users/x/.npm-global/bin/copilot --yolo` case → returns True. |
| 2026-05-23 | FX001-2 | insight | Live before-state: `discover_all()` reported `comax:node [copilot] uuid=None cwd=None`. Live after-state: `comax:node [pi] uuid=019e4d1e…` resolves correctly, 0 phantom copilots (was 1). 18 real copilot + 12 claude + 5 pi all still detect. | Fix validated end-to-end via reproducer + 11-case unit table + full-discovery regression check. |
